# 🚀 Інструкція з встановлення та запуску

## Крок 1: Встановлення MongoDB

### macOS:
```bash
# Встановити MongoDB через Homebrew
brew tap mongodb/brew
brew install mongodb-community

# Запустити MongoDB
brew services start mongodb-community
```

### Перевірка:
```bash
mongosh
# Має відкритися MongoDB shell
```
y
## Крок 2: Створення файлу .env

Створіть файл `.env` в корені проекту з таким вмістом:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/tripsforua
JWT_SECRET=my_super_secret_key_2024_change_in_production
NODE_ENV=development
ADMIN_EMAIL=admin@tripsforua.com
ADMIN_PASSWORD=admin123
```

## Крок 3: Встановлення залежностей

```bash
# Backend залежності
npm install

# Frontend залежності
cd client
npm install
cd ..
```

## Крок 4: Заповнення бази даних

```bash
npm run seed
```

Вивід має бути схожим на:
```
✅ MongoDB підключено
🗑️  Старі дані видалено
✅ Адмін створено: admin@tripsforua.com
✅ Напрямки створено: 8
✅ Тури створено: 5

🎉 База даних успішно заповнена!

👤 Дані для входу в адмін-панель:
   Email: admin@tripsforua.com
   Пароль: admin123
```

## Крок 5: Запуск проекту

```bash
npm run dev
```

Це запустить:
- Backend на http://localhost:5000
- Frontend на http://localhost:3000

## Крок 6: Відкрийте сайт

### Головна сторінка:
http://localhost:3000

### Адмін-панель:
http://localhost:3000/admin/login

**Дані для входу:**
- Email: `admin@tripsforua.com`
- Пароль: `admin123`

## 🔧 Можливі проблеми

### Помилка з'єднання з MongoDB:
```bash
# Перевірте чи працює MongoDB
brew services list

# Перезапустіть якщо потрібно
brew services restart mongodb-community
```

### Помилка з портами:
Якщо порт 3000 або 5000 зайнятий, змініть в:
- Backend: `.env` файл (PORT=5000)
- Frontend: `client/vite.config.js` (port: 3000)

### Помилка "Cannot find module":
```bash
# Видаліть node_modules та встановіть знову
rm -rf node_modules client/node_modules
npm install
cd client && npm install
```

## 📱 Тестування функціоналу

1. **Перегляд турів** - головна сторінка
2. **Деталі туру** - клік на будь-який тур
3. **Бронювання** - форма на сторінці туру
4. **Адмін-панель** - вхід через /admin/login
5. **Додавання туру** - в адмін-панелі
6. **Редагування** - клік на Edit біля туру
7. **Перегляд бронювань** - розділ Bookings в адмін-панелі

## 🎨 Зміна дизайну

Всі кольори налаштовані в `client/tailwind.config.js`:
```js
primary: {
  600: '#0284c7', // Основний синій колір
  // Змініть на свій
}
```

## 📝 Додавання нових напрямків

Через адмін-панель:
1. Увійдіть як адмін
2. Destinations → Add Destination
3. Заповніть всі поля
4. Знайдіть фото на Unsplash.com
5. Зберігайте

## ✅ Готово!

Тепер у вас є повнофункціональний сайт для продажу турів з адмін-панеллю!
