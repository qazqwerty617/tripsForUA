# 🔧 Виправлення проблеми з входом в адмінку

## Проблема
При спробі входу в адмін-панель з'являється помилка "Невірний email або пароль"

## Рішення

### Крок 1: Очистити базу даних і створити адміна знову

Виконайте наступні команди в терміналі:

```bash
# Перейдіть в папку проекту
cd /Users/qazqwerty617/Downloads/tripsForUA

# Запустіть seed скрипт знову
npm run seed
```

Це створить адміна з правильним хешованим паролем.

### Крок 2: Дані для входу

Після успішного виконання `npm run seed` ви побачите:

```
✅ Адмін створено: admin@tripsforua.com
✅ Напрямки створено: 8
✅ Тури створено: 5

🎉 База даних успішно заповнена!

👤 Дані для входу в адмін-панель:
   Email: admin@tripsforua.com
   Пароль: admin123
```

### Крок 3: Увійти в адмінку

1. Відкрийте: http://localhost:3000/admin/login
2. Введіть:
   - **Email**: `admin@tripsforua.com`
   - **Пароль**: `admin123`

## Якщо проблема залишилася

### Перевірка MongoDB

```bash
# Перевірте, чи працює MongoDB
brew services list | grep mongodb

# Якщо не працює, запустіть:
brew services start mongodb-community

# Підключіться до MongoDB
mongosh

# У MongoDB shell:
use tripsforua
db.users.find()

# Має показати адміна
```

### Альтернативне рішення: Видалити БД вручну

```bash
# У MongoDB shell (mongosh):
use tripsforua
db.dropDatabase()
exit

# Потім знову запустіть:
npm run seed
```

## Перевірка .env файлу

Переконайтеся, що у вас є файл `.env` в корені проекту з таким вмістом:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/tripsforua
JWT_SECRET=my_super_secret_key_2024_change_in_production
NODE_ENV=development
ADMIN_EMAIL=admin@tripsforua.com
ADMIN_PASSWORD=admin123
```

## ✅ Успішний вхід

Після успішного входу ви потрапите на Dashboard адмін-панелі з:
- Статистикою турів
- Кнопками керування
- Списком бронювань

---

**Якщо жодне з рішень не допомогло**, напишіть мені і я допоможу! 🚀
