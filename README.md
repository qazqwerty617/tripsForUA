# TripsForUA - Сайт для продажу турів

Повнофункціональний сайт для продажу турів з адмін-панеллю. Створено на React, Node.js, Express та MongoDB.

## 🌍 Напрямки

- Мадейра 🇵🇹
- Ісландія 🇮🇸
- Норвегія 🇳🇴
- Доломіти 🇮🇹
- Швейцарія 🇨🇭
- Кенія 🇰🇪
- Мадагаскар 🇲🇬
- Маврикій 🇲🇺

## ✨ Можливості

### Для відвідувачів:
- 🏠 Красива головна сторінка
- 🗺️ Перегляд напрямків та турів
- 📅 Календар майбутніх турів
- 💳 Онлайн бронювання
- 📱 Адаптивний дизайн

### Для адміністратора:
- ✏️ Додавання/редагування турів
- 🌍 Керування напрямками
- 📊 Перегляд бронювань
- 🔒 Захищена адмін-панель

## 🚀 Швидкий старт

### Вимоги
- Node.js >= 16
- MongoDB >= 5.0
- npm або yarn

### 1. Встановлення залежностей

```bash
# Встановити залежності для backend
npm install

# Встановити залежності для frontend
cd client
npm install
cd ..
```

### 2. Налаштування бази даних

Переконайтеся, що MongoDB запущено:

```bash
# macOS (через Homebrew)
brew services start mongodb-community

# Або запустіть вручну
mongod --config /usr/local/etc/mongod.conf
```

### 3. Налаштування змінних середовища

Створіть файл `.env` в корені проекту:

```bash
cp .env.example .env
```

Відредагуйте `.env` файл:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/tripsforua
JWT_SECRET=your_secure_secret_key_change_this
NODE_ENV=development
ADMIN_EMAIL=admin@tripsforua.com
ADMIN_PASSWORD=admin123
```

### 4. Заповнення бази даних

Запустіть скрипт для додавання тестових даних:

```bash
npm run seed
```

Це створить:
- Адміністратора (email: admin@tripsforua.com, пароль: admin123)
- 8 напрямків
- 5 прикладів турів

### 5. Запуск проекту

```bash
# Запустити backend та frontend одночасно
npm run dev
```

Або окремо:

```bash
# Тільки backend (порт 5000)
npm run server

# Тільки frontend (порт 3000)
npm run client
```

### 6. Відкрийте в браузері

- **Головна сторінка**: http://localhost:3000
- **Адмін-панель**: http://localhost:3000/admin/login

**Дані для входу:**
- Email: `admin@tripsforua.com`
- Пароль: `admin123`

## 📁 Структура проекту

```
tripsForUA/
├── client/                # React Frontend
│   ├── src/
│   │   ├── components/   # Компоненти (Navbar, Footer)
│   │   ├── pages/        # Сторінки
│   │   │   ├── admin/   # Адмін-панель
│   │   │   ├── Home.jsx
│   │   │   ├── Destinations.jsx
│   │   │   └── TourDetail.jsx
│   │   ├── store/        # Zustand store
│   │   ├── utils/        # API клієнт
│   │   └── App.jsx
│   └── package.json
├── server/               # Express Backend
│   ├── models/          # MongoDB моделі
│   │   ├── Tour.js
│   │   ├── Destination.js
│   │   ├── User.js
│   │   └── Booking.js
│   ├── routes/          # API маршрути
│   │   ├── tours.js
│   │   ├── destinations.js
│   │   ├── auth.js
│   │   └── bookings.js
│   ├── middleware/      # Middleware (auth)
│   ├── seed.js          # Скрипт заповнення БД
│   └── index.js         # Точка входу
├── .env.example         # Приклад змінних середовища
├── package.json
└── README.md
```

## 🔧 API Endpoints

### Публічні
- `GET /api/tours` - Список турів
- `GET /api/tours/:id` - Деталі туру
- `GET /api/destinations` - Список напрямків
- `GET /api/destinations/:slug` - Деталі напрямку
- `POST /api/bookings` - Створити бронювання
- `POST /api/auth/login` - Вхід

### Адмін (потрібна авторизація)
- `POST /api/tours` - Створити тур
- `PUT /api/tours/:id` - Оновити тур
- `DELETE /api/tours/:id` - Видалити тур
- `POST /api/destinations` - Створити напрямок
- `PUT /api/destinations/:id` - Оновити напрямок
- `DELETE /api/destinations/:id` - Видалити напрямок
- `GET /api/bookings` - Список бронювань
- `PUT /api/bookings/:id` - Оновити статус бронювання

## 🎨 Технології

### Frontend
- **React 18** - UI фреймворк
- **React Router** - Маршрутизація
- **TailwindCSS** - Стилізація
- **Lucide React** - Іконки
- **Axios** - HTTP клієнт
- **Zustand** - State management
- **React Hot Toast** - Повідомлення
- **date-fns** - Робота з датами
- **Vite** - Збірка

### Backend
- **Node.js** - Runtime
- **Express** - Web фреймворк
- **MongoDB** - База даних
- **Mongoose** - ODM
- **JWT** - Авторизація
- **bcryptjs** - Хешування паролів

## 📝 Основні функції

### Адмін-панель

1. **Керування турами**
   - Додавання нових турів
   - Редагування існуючих
   - Встановлення цін та дат
   - Керування зображеннями
   - Додавання програми туру

2. **Керування напрямками**
   - Створення нових країн
   - Опис та фото
   - Галерея зображень
   - Встановлення популярності

3. **Бронювання**
   - Перегляд всіх бронювань
   - Зміна статусу
   - Відстеження оплати
   - Контактні дані клієнтів

## 🔐 Безпека

- JWT токени для авторизації
- Bcrypt для хешування паролів
- Захищені маршрути адміністратора
- CORS налаштування
- Валідація даних

## 🌐 Продакшн

### Збірка для продакшну

```bash
# Збудувати frontend
cd client
npm run build
cd ..

# Запустити сервер
NODE_ENV=production npm start
```

### Змінні середовища для продакшну

```env
NODE_ENV=production
PORT=5000
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/tripsforua
JWT_SECRET=very_secure_random_string_change_this
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=secure_password
```

## 📞 Підтримка

Для питань та підтримки зв'яжіться з розробником.

## 📄 Ліцензія

Цей проект створено для навчальних цілей.

---

**Зроблено з ❤️ для мандрівників України**
