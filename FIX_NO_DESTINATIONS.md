# 🔧 Виправлення: Країни не з'являються

## Проблема
Країни не відображаються в секції "Популярні напрямки"

---

## ✅ Рішення: Крок за кроком

### 1️⃣ Перевірте MongoDB

```bash
# Перевірити чи працює MongoDB
ps aux | grep mongod

# Якщо не працює, запустіть:
mongod --dbpath ~/data/db

# АБО якщо встановлено через brew:
brew services start mongodb-community
```

---

### 2️⃣ Заповніть базу даних

**Відкрийте НОВИЙ термінал** і виконайте:

```bash
cd /Users/qazqwerty617/Downloads/tripsForUA
npm run seed
```

Ви маєте побачити:
```
✅ MongoDB підключено
🗑️  Старі дані видалено
✅ Адмін створено: admin@tripsforua.com
✅ Напрямки створено: 8
✅ Тури створено: 5
```

---

### 3️⃣ Запустіть проект

```bash
# Зупиніть старий процес (якщо працює)
# Натисніть Ctrl+C в терміналі з npm run dev

# Запустіть знову
npm run dev
```

---

### 4️⃣ Перевірте браузер

1. Відкрийте: http://localhost:3000
2. Натисніть **F12** (відкрити консоль)
3. Перейдіть на вкладку **Console**
4. Подивіться на помилки

**Що шукати:**
- ❌ `Error: connect ECONNREFUSED` → MongoDB не запущений
- ❌ `404 Not Found` → Backend не запущений
- ✅ `Destinations: Array(8)` → Все працює!

---

### 5️⃣ Якщо досі не працює

**Перевірте чи працює Backend:**

```bash
# Відкрийте новий термінал
curl http://localhost:5000/api/destinations
```

Ви маєте побачити JSON з 8 країнами.

**Якщо отримуєте помилку:**

```bash
# Перевірте чи порт 5000 вільний
lsof -ti:5000

# Вбийте процес якщо потрібно
lsof -ti:5000 | xargs kill -9

# Запустіть backend окремо
npm run server
```

---

## 🎯 Швидке виправлення (все в одному)

```bash
# 1. Зупиніть всі процеси
lsof -ti:5000 | xargs kill -9
lsof -ti:3000 | xargs kill -9

# 2. Запустіть MongoDB (оберіть один варіант)
# Варіант A: Якщо встановлено через brew
brew services start mongodb-community

# Варіант B: Якщо встановлено вручну
mongod --dbpath ~/data/db &

# 3. Заповніть базу
cd /Users/qazqwerty617/Downloads/tripsForUA
npm run seed

# 4. Запустіть проект
npm run dev
```

---

## 📝 Перевірочний список

- [ ] MongoDB запущений
- [ ] `npm run seed` виконано успішно
- [ ] Backend працює на порту 5000
- [ ] Frontend працює на порту 3000
- [ ] Консоль браузера показує `Destinations: Array(8)`
- [ ] На сайті видно 8 країн: 🇵🇹 🇮🇸 🇳🇴 🇮🇹 🇨🇭 🇰🇪 🇲🇬 🇲🇺

---

## ❓ Все ще не працює?

**Надішліть мені інформацію:**

```bash
# Запустіть ці команди і покажіть результат:
echo "=== MongoDB Status ==="
ps aux | grep mongod

echo "=== Node Processes ==="
ps aux | grep node

echo "=== Backend API Test ==="
curl http://localhost:5000/api/destinations

echo "=== Frontend Status ==="
curl http://localhost:3000
```

Тоді я зможу точно сказати, що не так! 🔍
